package com.example.training.repository;

import com.example.training.model.po.ChannelTagMapping;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChannelTagMappingRepository extends JpaRepository<ChannelTagMapping, Long> {
}
